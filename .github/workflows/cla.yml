name: 📜 Contribution Guidelines

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  sign:
    uses: hotosm/gh-workflows/.github/workflows/cla_sign.yml@3.6.0
    with:
      push_branch: dev
    secrets: inherit
