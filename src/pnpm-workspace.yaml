packages:
  - "frontend"
  - "gcp-editor"
# Only the packages that build or fetch a native binary.
allowBuilds:
  canvas: true
  core-js: false
  core-js-pure: false
  esbuild: true
  exifreader: false
minimumReleaseAgeExclude:
  - '@hotosm/ui@0.10.2 || 1.0.0 || 1.0.1 || 2.0.0 || 2.1.0'
